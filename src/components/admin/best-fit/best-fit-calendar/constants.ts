/** Height (px) of every 30-minute time row. */
export const SLOT_HEIGHT = 72;
/** Width of the left time-gutter column (px). */
export const TIME_GUTTER_W = 60;
/** How many doctor chips to render before showing "+N more". */
export const MAX_CHIPS = 4;
/** Chip limit when a day column is expanded. */
export const MAX_CHIPS_EXPANDED = 6;
