services:
  postgres:
    image: postgres:18.0
    container_name: postgres_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: riaya
    ports:
      - "5432:5432"
    volumes:
      - riaya_postgres_data:/var/lib/postgresql

volumes:
  riaya_postgres_data:
    name: riaya_postgres_data